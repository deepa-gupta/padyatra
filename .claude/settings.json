{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); case \"$cmd\" in *'git commit'*) bash /Users/kyra/Coding/padyatra/.git/hooks/pre-commit; echo '{\"continue\":true}' ;; *) echo '{\"continue\":true}' ;; esac",
            "timeout": 300,
            "statusMessage": "Running CLAUDE.md checks + tests…"
          }
        ]
      }
    ]
  }
}
